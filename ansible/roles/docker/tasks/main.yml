- name: Install Docker
  apt:
    name: docker.io
    state: present

- name: Enable Docker
  service:
    name: docker
    state: started
    enabled: true

- name: Add ubuntu to docker group
  user:
    name: ubuntu
    groups: docker
    append: yes
